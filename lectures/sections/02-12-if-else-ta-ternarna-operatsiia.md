---
chapter: 2
chapterTitle: "Розділ 2. Основи програмування на C#"
section: 12
number: "2.12"
title: "Конструкція if..else та тернарна операція"
source: "../_migration/source-chunks/07-umovni-vyrazy-ta-cykly.md"
---

## 2.12. Конструкція if..else та тернарна операція

Умовні вирази (розділ 2.11) повертають `true` або `false` — але самі по собі нічого не роблять. Щоб програма реагувала на результат перевірки і виконувала різні дії залежно від умови, використовуються **умовні конструкції**. Найпоширеніша з них — `if...else`.

![Конструкція if/else та тернарна операція](_assets/02-12/if-else-ternary.png)

## Блок if

Найпростіша форма — блок `if` без гілки `else`. Якщо умова в дужках дорівнює `true`, виконується тіло блоку; якщо `false` — тіло пропускається і програма продовжується далі:

```csharp
if (умова)
{
    // виконується лише коли умова == true
}
```

```csharp run
using System;

int bpSystolic = 165;

if (bpSystolic >= 160)
{
    Console.WriteLine("Увага: критично підвищений артеріальний тиск!");
    Console.WriteLine("Необхідна негайна консультація лікаря.");
}

Console.WriteLine("Огляд завершено.");
```

Якщо тіло блоку складається лише з однієї інструкції, фігурні дужки можна опустити. Проте для читабельності та уникнення помилок краще завжди їх писати:

```csharp
if (bpSystolic >= 160)
    Console.WriteLine("Критично підвищений тиск!");  // одна інструкція
```

## Блок else

Блок `else` виконується, якщо умова блоку `if` дорівнює `false`. Таким чином одна з двох гілок завжди виконається:

```csharp run
using System;

int age = 15;

if (age >= 18)
{
    Console.WriteLine("Пацієнт повнолітній. Реєстрація без обмежень.");
}
else
{
    Console.WriteLine("Пацієнт неповнолітній. Потрібна згода батьків.");
}
```

## Ланцюжок else if

Коли варіантів більше двох, до конструкції додаються гілки `else if`. CLR перевіряє умови **зверху вниз** і виконує першу гілку, умова якої є `true`. Решта гілок при цьому ігноруються:

```csharp run
using System;

int bpSystolic = 145;

if (bpSystolic < 120)
{
    Console.WriteLine("Тиск: оптимальний.");
}
else if (bpSystolic < 130)
{
    Console.WriteLine("Тиск: нормальний.");
}
else if (bpSystolic < 140)
{
    Console.WriteLine("Тиск: підвищено-нормальний. Спостереження.");
}
else if (bpSystolic < 160)
{
    Console.WriteLine("Тиск: гіпертонія I ступеня. Консультація.");
}
else if (bpSystolic < 180)
{
    Console.WriteLine("Тиск: гіпертонія II ступеня. Лікування.");
}
else
{
    Console.WriteLine("Тиск: гіпертонічний криз! Екстрена допомога!");
}
```

Останній блок `else` є необов'язковим, але виступає «страхувальною» гілкою, що обробляє всі інші випадки. В медичних системах рекомендується завжди його вказувати.

## Вкладені if

Умовні конструкції можна вкладати одна в одну. При цьому важливо стежити за відступами, щоб не заплутатись у структурі:

```csharp run
using System;

int age        = 72;
int bpSystolic = 170;

if (age >= 60)
{
    Console.WriteLine("Пацієнт: похилого віку.");

    if (bpSystolic >= 160)
    {
        Console.WriteLine("Стан: підвищений ризик. Госпіталізація.");
    }
    else
    {
        Console.WriteLine("Стан: стабільний. Профілактичне спостереження.");
    }
}
else
{
    Console.WriteLine("Пацієнт: молодший вікової групи ризику.");
}
```

## Тернарна операція

Тернарна (умовна) операція є компактним варіантом `if...else` для **виразів** — тобто там, де потрібно обрати одне з двох значень залежно від умови. Синтаксис:

```text
умова ? значення_якщо_true : значення_якщо_false
```

Оператор `?` приймає три операнди, звідки й назва «тернарний». Якщо умова `true` — результатом є другий операнд; якщо `false` — третій:

```csharp run
using System;

int age = 45;

string ageGroup = age >= 60 ? "Пенсіонер" : "Дорослий";
Console.WriteLine($"Вікова група: {ageGroup}");

int bpSystolic = 145;
string bpStatus = bpSystolic >= 140 ? "Підвищений" : "Нормальний";
Console.WriteLine($"Тиск: {bpStatus} ({bpSystolic.ToString()} мм рт.ст.)");
```

Тернарний оператор зручний для коротких виразів. Для складних умов або кількох дій краще використовувати повну конструкцію `if...else` — це підвищує читабельність і спрощує підтримку коду.

Тернарні оператори можна вкладати один в одний, але більше одного рівня вкладення суттєво знижує зрозумілість:

```csharp run
using System;

int bpSystolic = 145;

string bpCategory = bpSystolic < 120 ? "Оптимальний"
                  : bpSystolic < 140 ? "Нормальний"
                  : bpSystolic < 160 ? "Гіпертонія I"
                  : "Гіпертонія II+";

Console.WriteLine($"Категорія тиску: {bpCategory}");
```

## Умова як значення bool

В умові блоку `if` може стояти будь-який вираз, що повертає `bool` — у тому числі змінна типу `bool`, результат порівняння або повернене значення методу:

```csharp run
using System;

bool isEmergency   = true;
int  bpSystolic    = 185;

if (isEmergency)
    Console.WriteLine("Режим екстреної допомоги активовано.");

if (isEmergency && bpSystolic > 180)
    Console.WriteLine("Гіпертонічний криз — виклик реанімаційної бригади!");
```

Ніколи не пишіть `if (isEmergency == true)` — це надлишок. `if (isEmergency)` і `if (!isEmergency)` — коректний і лаконічний стиль.
